# Documentation site (MkDocs + Material). Build/preview with:
#   python3 -m venv .venv-docs && .venv-docs/bin/pip install -r requirements-docs.txt
#   .venv-docs/bin/mkdocs serve      # preview at http://127.0.0.1:8000
#   .venv-docs/bin/mkdocs build --strict
#   .venv-docs/bin/mkdocs gh-deploy  # publish to the gh-pages branch
mkdocs-material==9.7.7
